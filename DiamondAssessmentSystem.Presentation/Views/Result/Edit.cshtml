@model DiamondAssessmentSystem.Application.DTO.ResultCreateDto
@using DiamondAssessmentSystem.Application.DTO
@{
    ViewData["Title"] = "Edit Result";
}

<div class="max-w-3xl mx-auto py-10 px-4">
    <h1 class="text-3xl font-bold text-gray-800 mb-6">@ViewData["Title"]</h1>

    <form asp-action="Edit" method="post" class="space-y-6 bg-white p-6 shadow-md rounded-xl border">
        <div asp-validation-summary="ModelOnly" class="text-red-500 text-sm"></div>

        @foreach (var prop in typeof(ResultCreateDto).GetProperties())
        {
            var name = prop.Name;
            <div>
                <label asp-for="@name" class="block font-semibold text-gray-700">@name</label>
                <input asp-for="@name" class="w-full mt-1 border-gray-300 rounded-md shadow-sm" />
                <span asp-validation-for="@name" class="text-sm text-red-500"></span>
            </div>
        }

        <div class="flex justify-between items-center mt-4">
            <a asp-action="Index" class="text-sm text-blue-600 hover:underline">← Back to List</a>
            <button type="submit" class="bg-yellow-500 text-white px-4 py-2 rounded hover:bg-yellow-600 transition">Save Changes</button>
        </div>
    </form>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}
